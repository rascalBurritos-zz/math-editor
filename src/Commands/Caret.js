export default class Caret {
  _anchorIndices;

  /**
   * @param {Array} arr indices in root node
   */
  set anchorIndices(arr) {
    this._anchorIndices;
  }
  /**
   * @return {Array} indices in root node
   */
  get anchorIndices() {
    return this._anchorIndices;
  }
}
