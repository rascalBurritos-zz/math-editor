import {FormulaComponentData} from './FormulaComponentData.js'
export class radicalComponentData {
    constructor(){
        this.component = Radical;
        this.css = {display:'flex',flexDirection:'row'}
        var radicalInfo = retrieveRadicalInfo(fontData)
        this.radicand = new FormulaComponentData(mathList,fontData,radicandStyle)
        this.delimiter = radicalComponentData.getDelimiter(desiredSize)
        thi
        var centeringSpace = calculateCenteringSpace(delimiterSize,radicand,minClearance);
        

    }

}
